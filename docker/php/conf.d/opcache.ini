[opcache]
; Determines if Zend OPCache is enabled
opcache.enable=1

; How often (in seconds) to check file timestamps for changes to the shared
; memory storage allocation. ("1" means validate once per second, but only
; once per request. "0" means always validate)
opcache.revalidate_freq=0

; When disabled, you must reset the OPcache manually or restart the
; webserver for changes to the filesystem to take effect.
opcache.validate_timestamps=0

; The maximum number of keys (scripts) in the OPcache hash table.
; Only numbers between 200 and 1000000 are allowed.
opcache.max_accelerated_files=10000

; The OPcache shared memory storage size.
opcache.memory_consumption=192

; The maximum percentage of "wasted" memory until a restart is scheduled.
opcache.max_wasted_percentage=10

; The amount of memory for interned strings in Mbytes.
opcache.interned_strings_buffer=16

; The strategy used for JIT. The "tracing" mode corresponds to CRTO = 1254.
; Disabled: PHP 8.5 ZTS + opcache JIT on aarch64-alpine segfaults under load (mixed SIGSEGV/SIGBUS).
opcache.jit=off

; The amount of shared memory to reserve for compiled JIT code. A zero value disables the JIT.
opcache.jit_buffer_size=0

; Enables or disables copying of PHP code (text segment) into HUGE PAGES.
; Set it to `0` for AArch64 (ARM64), since it's not supported
; Set it to `1` for Linux (x86_64) to improve performance.
opcache.huge_code_pages=0
